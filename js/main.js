// eventually include something silly like an input that serves no purpose except to change the background 
// color overlay on keyups. Will need to include something for opacity so it doesn't completely cover up the 
// existing background. 

// function randomColorChange(){
//   return '#'+(Math.floor(Math.random()*16777216)&0xFFFFFF).toString(16);
// }
// $('#temp').on('blur keyup', function() {
//   $('body').css('background-color', randomColorChange());
// });

